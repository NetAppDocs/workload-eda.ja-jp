---
sidebar: sidebar 
permalink: task-use-overview-dashboard.html 
keywords: cluster overview, filter dashboard, cluster health, capacity recommendations, SSD management, bulk edit, decision points, clusters table, ONTAP events, latency events 
summary: 概要ダッシュボードを使用して、クラスタの健全性を監視し、容量に関する推奨事項を表示し、SSD容量設定を管理し、FSx for ONTAPファイルシステム全体のパフォーマンス傾向を分析します。 
---
= Workload Factory for EDA の概要ダッシュボードを使用する
:allow-uri-read: 
:icons: font
:imagesdir: ./media/


[role="lead"]
概要ダッシュボードを使用して、FSx for ONTAP ファイルシステム全体のクラスターの健全性と容量を監視します。データのフィルタリング、容量の推奨事項の表示、SSD 容量設定の管理、パフォーマンス傾向の分析により、ボリュームの配置と容量の拡張について十分な情報に基づいた意思決定を行います。



== 開始する前に

ファイルシステムを監視するために使用する前に、link:task-activate-overview-dashboard.html["概要ダッシュボードをアクティブ化しました"]を実行しておく必要があります。



== ダッシュボードをフィルタリングする

ダッシュボード上部のフィルターを使用して、特定のファイルシステムに絞り込んでください。これらのフィルターは、レイテンシ、使用率、およびONTAPイベントセクションとクラスターテーブルに適用されます。

.手順
. 次のいずれかを使用してログインします link:https://docs.netapp.com/us-en/workload-setup-admin/console-experiences.html["コンソール体験"^]。
. メニューimage:workloads-menu-icon.png["ハンバーガーメニューアイコン"]を選択し、*EDA*を選択します。
. * Overview *タブを選択します。
. 利用可能なフィルタを使用して、表示を絞り込みます。
+
** *AWSアカウント*：設定済みの認証情報に関連付けられている1つ以上のAWSアカウントでフィルタリングします
** *認証情報*：複数のAWS認証情報を設定している場合は、特定の認証情報でフィルタリングしてください
** *リージョン*：1つ以上のAWSリージョンでフィルタリングします


. フィルターの選択を更新すると、一致するファイルシステムのみが表示されるように、すべての情報が更新されます。




== クラスタの健全性ステータスを表示する

ダッシュボードの上部には、フィルタリングされたファイルシステム全体の健全性とアクティビティの概要が表示されます。この情報は、少なくとも1つのFSx for ONTAPリンクがファイルシステムに関連付けられている場合にのみ表示されます。

.タスク概要
クラスタの健全性ステータスには、次の主要な領域が含まれます。

* *レイテンシ*：検出されたレイテンシイベントの数
* *SSD容量モード*：SSDの使用状況とアクティブな容量推奨事項があるファイルシステム
* *ONTAPイベント*：カテゴリ別（容量、可用性と保護、セキュリティ、その他）のEMSイベント数


.手順
. ダッシュボード上部のクラスタの健全性ステータスを確認します。
. レイテンシイベントを表示するには：
+
** *Review*を選択して*Latency*タブに移動します
** レイテンシ監視が設定されていない場合は、*Configure* を選択してレイテンシのしきい値を設定してください
+
レイテンシ監視の詳細については、link:task-configure-latency-monitoring.html["レイテンシー監視の設定"^]を参照してください。



. FSx for ONTAP ファイルシステムの ONTAP イベントを表示するには、*Review* を選択します。
+
ONTAP イベントビューカードには、カテゴリ別（容量、可用性と保護、セキュリティとその他）の EMS イベント数が表示されます。監視対象の EMS イベントの全リストについては、link:https://docs.netapp.com/us-en/ontap-cli-9171/index.html["ONTAP CLI ドキュメント"^]を参照してください。





== 容量に関する推奨事項を表示して適用する

Workload Factory は、FSx for ONTAP ファイルシステムごとに容量推奨アルゴリズムを自動的に実行します。クラスタテーブルをフィルタリングすることで、容量に関する推奨事項が設定されているファイルシステムを特定できます。

.手順
. * Clusters *テーブルで、* Last SSD increase timestamp *列または* Last SSD increase description *列でフィルタリングします。
+
容量に関する推奨事項が記載されているファイルシステムは、表の一番上に表示されます。

. 表からファイルシステム名を選択すると、そのファイルシステムが開き、推奨事項が表示されます。
. SSDの推奨仕様と容量グラフを確認してください。
+
この勧告では、提案された変更内容とその理由について説明しています。例：_ファイルシステムSSDの使用パターンに基づいて、SSDサイズを増やすことをお勧めします。_

+
このグラフは、現在のSSD使用状況と過去の傾向を並べて表示し、容量推奨アルゴリズムが時間の経過とともにどのように容量を調整してきたかを示しています。

. グラフの右上で、表示期間を変更する時間範囲を選択してください。デフォルトは1週間です。
+
グラフには、推奨決定ポイントと自動化決定ポイント（自動化モードが有効になっている場合）も表示されます。任意の決定ポイントにカーソルを合わせると、その時点での容量の拡張に関する推奨事項または自動化アクションの詳細が表示されます。

. 推奨事項を適用するには、推奨事項メッセージに記載されているガイダンスに従ってください。




== SSD管理モードを変更する

個々のファイルシステムのSSD容量管理モードを変更して、手動による推奨事項と自動による容量の拡張のどちらを使用するかを制御できます。

.タスク概要
SSD管理モードには以下が含まれます：

* *自動化*：事前定義されたしきい値に基づいて、SSD容量を自動的に増加します。
* *推奨*：容量に関する推奨事項を受け取り、手動で確認して適用します。
* *なし*：容量に関する推奨事項や自動アクションはありません


自動化モードを選択すると、容量管理システムがSSDの使用状況を分析し、自動化の判断を行う方法を制御するしきい値、参照期間、および先読みパラメータを設定できます。推奨モードを選択した場合、しきい値パラメータのみを設定できます。

* *しきい値*（10～90％）：容量の推奨または自動化アクションをトリガーするSSD使用率
* *Lookback*（1～200時間）：過去のSSD使用パターンを分析するために使用される期間
* *今後*（1～200時間）：将来の容量ニーズを予測するために使用される期間


.手順
. Clustersテーブルでファイルシステム名を確認します。その横にあるimage:icon-action.png["アクションメニューアイコン"]を開き、*Change SSD capacity management*を選択します。
. お好みのモードを選択してください：
+
** *なし*：容量管理を無効にする
** *推奨*：推奨分析のしきい値パラメータを設定します
** *自動化*：自動容量の拡張のために、しきい値、ルックバック、およびアヘッドのパラメーターを設定します


. *適用*を選択します。


.結果
ファイルシステムは新しいモードを即座に使用します。「推奨」または「自動化」を選択した場合、システムは設定されたパラメータを使用して容量分析と意思決定を行います。



== SSD管理パラメータの一括編集

複数のファイルシステムに対してSSD容量の拡張管理パラメータを同時に調整することで、クラスター全体に一貫した設定を適用できます。

.タスク概要
複数のファイルシステムを選択して一括編集する場合：

* 選択されたすべてのファイルシステムのパラメータ値が同一である場合、それらの値は編集ダイアログにデフォルト値として表示されます
* 選択されたファイルシステムのいずれかが None モードの場合、ダイアログにデフォルト値が表示されます
* 選択したファイルシステム間でパラメータ値が異なる場合、ダイアログにはデフォルト値が表示されます


.手順
. *Overview* タブから、表内で編集したいファイルシステムを選択します。
+

TIP: 個々のファイルシステムを選択することも、ヘッダーのチェックボックスを使用して表示されているすべてのファイルシステムを選択することもできます。

. 表の上にある *SSD容量管理の変更* を選択します。
. お好みのモードを選択してください。
. 編集ダイアログで、必要に応じてThreshold、Lookback、およびAheadの各パラメーターを調整してください。
+
各パラメータフィールドには、その目的と有効範囲を説明するツールチップが含まれています。このダイアログには、選択内容に基づいた現在の値またはデフォルト値が表示されます。

. *適用*を選択すると、選択したすべてのファイルシステムに変更が適用されます。


.結果
選択されたすべてのファイルシステムのパラメータが更新されます。推奨モードまたは自動化モードのファイルシステムは、容量の拡張と意思決定のために新しいパラメータを即座に使用します。



== 容量の傾向と意思決定ポイントを分析する

SSD使用状況グラフを使用して、時間の経過に伴う容量の挙動を把握し、容量管理におけるパターンを特定します。

.タスク概要
グラフ上の決定点は、次の時点を示します：

* *推奨決定ポイント*：容量推奨アルゴリズムにより、追加のSSD容量が必要であることが判明しました。SSD容量が増やされていない場合、これらは30分ごとに発生する可能性があります。
* *自動化の決定ポイント*：自動化システムはSSD容量の増加を試みました。これらの点は、その行動が成功したか失敗したかを示しています。


.手順
. *Overview* タブから、表内のファイルシステム名を選択します。
. SSD使用状況グラフで過去の容量推移を確認してください。
. 異なる期間を選択して、さまざまな期間における容量の動作を確認できます。
. グラフ上の決定ポイントにカーソルを合わせると、特定の時点における推奨事項や自動化アクションの詳細が表示されます。
. グラフから得られる知見を活用して、次のことを行います。
+
** 容量調整がどのくらいの頻度で必要になるかを理解する
** 自動化モードと推奨モードのどちらがワークロードパターンに適しているかを評価してください。
** 繰り返し発生する容量制約を特定する
** 成長傾向に基づいて将来の容量ニーズを計画する
** 自動化の失敗に関するトラブルシューティング






== クラスタテーブルを使用する

クラスターテーブルには、パフォーマンスと使用状況の指標を含む各FSx for ONTAPファイルシステムの詳細なビューが表示されます。

.手順
. * Overview * タブから、クラスタテーブルを確認します。
. 検索ボックスを使用して、名前で特定のファイルシステムを検索します。
. データをソートするには、列ヘッダーを選択します。
. *SSD使用量*を使用して、容量制限に近づいているファイルシステムを特定します。
. *スループット使用量（P99）*を使用して、スループットの需要とプロビジョニングされたスループットSKUを比較します。
+
計算の詳細については、スループット列のヘッダーにカーソルを合わせてください。

. 列をカスタマイズするには、列設定アイコンを選択し、表示する列を選択します。
. データをエクスポートするには、エクスポートアイコンを選択してCSVファイルをダウンロードします。
. 一括操作を実行するには、チェックボックスを使用して複数のファイルシステムを選択し、次に*パラメーターの編集*を選択します。




== ベストプラクティス

概要ダッシュボードを使用する際は、以下の推奨事項を考慮してください：

* *定期的な監視*：ダッシュボードを定期的に確認し、問題になる前に傾向を把握してください。
* *適切なしきい値を設定する*：ワークロードのパターンと容量計画の好みに合わせて、SSD管理のしきい値を設定します。
* *フィルターを効果的に活用する*：地域フィルターとアカウントフィルターを組み合わせて、インフラストラクチャの特定のサブセットに焦点を当てます。
* *推奨事項を速やかに確認してください*：クラスタテーブルを、最後のSSD増加のタイムスタンプまたは説明でフィルタリングして、容量に関する推奨事項があるファイルシステムを特定します。容量制約を回避するため、これらの推奨事項を検討し、実行に移してください。
* *適切なモードを選択してください*：容量決定を自分でコントロールしたい場合は推奨モードを、管理を自動化したい場合は自動化モードを使用してください。
* *意思決定ポイントの監視*：容量グラフ上の意思決定ポイントを定期的に確認し、アルゴリズムの動作を理解し、設定がニーズに合致していることを検証します。
* *一括編集を活用する*：複数のファイルシステムを管理する場合は、パラメータの一括編集を使用して、一貫性のある容量管理ポリシーを維持します。




== 関連情報

* link:learn-overview-dashboard.html["概要ダッシュボードについて"]
* link:task-activate-overview-dashboard.html["概要ダッシュボードを有効にする"]
* link:task-configure-latency-monitoring.html["レイテンシー監視の設定"]

